PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=fashique
JWT_SECRET=fashique-change-me-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Comma-separated browser origins allowed by CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3002,http://127.0.0.1:3000

# Optional SMTP (order emails). Leave empty to skip sending in dev.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
